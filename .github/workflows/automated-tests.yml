name: PHP Composer

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Start pre-installed MySQL
        run: |
          sudo systemctl start mysql
          mysql -u root -proot -e "ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '';"
          mysql -u root -e "CREATE DATABASE IF NOT EXISTS myhmsdb;"
          mysql -u root myhmsdb < myhmsdb.sql
          mysql -u root -e "CREATE DATABASE IF NOT EXISTS myhmsdb_test;"
          mysql -u root myhmsdb_test < myhmsdb.sql
<<<<<<< HEAD
      - name: Apply schema migrations
        run: |
          mysql -u root myhmsdb <<'SQL'
          ALTER TABLE patreg  MODIFY password  VARCHAR(255) NOT NULL;
          ALTER TABLE patreg  MODIFY cpassword VARCHAR(255) NOT NULL;
          ALTER TABLE doctb   MODIFY password  VARCHAR(255) NOT NULL;
          ALTER TABLE admintb MODIFY password  VARCHAR(255) NOT NULL;
          ALTER TABLE appointmenttb ADD COLUMN payment VARCHAR(20) DEFAULT 'pending';
          SQL
          mysql -u root myhmsdb_test <<'SQL'
          ALTER TABLE patreg  MODIFY password  VARCHAR(255) NOT NULL;
          ALTER TABLE patreg  MODIFY cpassword VARCHAR(255) NOT NULL;
          ALTER TABLE doctb   MODIFY password  VARCHAR(255) NOT NULL;
          ALTER TABLE admintb MODIFY password  VARCHAR(255) NOT NULL;
          ALTER TABLE appointmenttb ADD COLUMN payment VARCHAR(20) DEFAULT 'pending';
          SQL

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'

      - name: Validate composer.json and composer.lock
        run: composer validate
=======

      - name: Validate composer.json
        run: composer validate --no-check-lock
>>>>>>> abfc8f3b52520438365bab7eb17f1b62390ea40f

      - name: Cache Composer packages
        id: composer-cache
        uses: actions/cache@v4
        with:
          path: vendor
          key: ${{ runner.os }}-php-${{ hashFiles('**/composer.json') }}
          restore-keys: |
            ${{ runner.os }}-php-
      - name: Install dependencies
        run: composer install --prefer-dist --no-progress

      - name: Run test suite
<<<<<<< HEAD
        run: php vendor/bin/phpunit
=======
        run: php vendor/bin/phpunit --testdox
>>>>>>> abfc8f3b52520438365bab7eb17f1b62390ea40f
